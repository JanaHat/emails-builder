{
  "version": 2,
  "buildCommand": "npm run build:all",
  "functions": {
    "api/server.js": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/server.js" }
  ]
}
